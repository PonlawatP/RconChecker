name: RconChecker
version: ${project.version}
main: pcus.rconchecker.RconChecker
authors: [Ponlawat]
description: check and keep that command if player is not online'
api-version: 1.13
commands:
  rcon:
    description: for Rcon sender only